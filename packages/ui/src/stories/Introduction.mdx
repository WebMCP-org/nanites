import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Introduction" />

# Sigvelo component library

This is a comprehensive component library built on [Base UI](https://base-ui.com/) for accessible, framework-agnostic React components.

## Features

- **Accessible by Default**: Built on Base UI's unstyled, accessible primitives
- **Framework-Friendly**: Works with Astro, Next.js, Remix, and any React framework
- **Themeable**: Built-in dark/light theme support using CSS variables
- **Type-Safe**: Full TypeScript support
- **No Tailwind**: Pure CSS styling with design tokens

## Components

Explore the components in the sidebar:

- **Button** - Multiple variants and sizes
- **Input** - Form inputs with focus states
- **Checkbox** - Accessible checkboxes with state management
- **Card** - Container component with optional hover effect
- **Label** - Form labels

## Theming

The design system supports both light and dark themes. Theme is controlled by adding/removing the `.dark` class on the root HTML element:

```javascript
// Light mode
document.documentElement.classList.remove("dark");

// Dark mode
document.documentElement.classList.add("dark");
```

## Design Tokens

All colors, spacing, typography, and other design tokens are defined as CSS variables in `/src/styles/index.css`.

You can customize them in your app by overriding the variables:

```css
:root {
  --accent: #your-color;
  --space-md: 1.5rem;
}
```

## Resources

- https://base-ui.com/llms.txt
- https://www.designtokens.org/tr/drafts/format/
- https://ui.shadcn.com/docs/theming
- https://skills.sh/vercel-labs/agent-skills/web-design-guidelines
