---
description: Test a SigVelo Nanite through the MCP acceptance loop.
---

Use the `nanites` skill. Test this Nanite or scenario: $ARGUMENTS. Prefer `sigvelo_test_nanite_trigger` for generated trigger code, with a raw GitHub webhook-shaped event that satisfies repository, branch, action, and path filters. Use `sigvelo_start_nanite_run` for manual behavior. Treat terminal run status and `agentFeedback` as the acceptance result.
