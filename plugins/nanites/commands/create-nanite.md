---
description: Create or update a Sigvelo Nanite through MCP.
---

Use the `nanites` skill. Confirm Sigvelo MCP access with `sigvelo_whoami`, inspect existing Nanites with `sigvelo_debug_nanites`, draft a thin manifest for: $ARGUMENTS, register with `sigvelo_create_nanite`, and test with `sigvelo_poke_nanite`.
