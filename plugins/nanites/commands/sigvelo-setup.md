---
description: Verify Sigvelo, GitHub, and Cloudflare setup.
---

Use the `nanites` skill. Verify the environment described by: $ARGUMENTS. Check GitHub CLI identity, Cloudflare/Wrangler access if deployment or provisioning is involved, the Sigvelo MCP endpoint from `.mcp.json`, and Sigvelo MCP auth with `sigvelo_whoami`. Confirm `nanites:read` for inspection and `nanites:write` before create, test, cancel, reset, or deprovision workflows.
