# Intentionally empty.
#
# Cloudflare Deploy to Cloudflare may read `.env.example` as deploy-time secret prompts.
# Keep first-run self-hosted deploys prompt-free. For local Sentry/browser build settings, copy
# `docs/env.local.example` to `.env`.
