import { API_PREFIX } from "./openapi-document.ts";

export const AUTH_INSTALLATION_ACTIVE_REPOSITORIES_ROUTE =
  "/auth/installations/active/repositories";

export const AUTH_INSTALLATION_ACTIVE_REPOSITORIES_PATH = `${API_PREFIX}${AUTH_INSTALLATION_ACTIVE_REPOSITORIES_ROUTE}`;

export const GITHUB_WEBHOOK_PATH = `${API_PREFIX}/github/webhook`;
